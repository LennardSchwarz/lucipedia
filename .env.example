# Lucipedia configuration example
# Copy this file to .env and adjust values as needed.

# Absolute or relative path to the SQLite database file.
DB_PATH=./wikipedai.db

# Port the HTTP server listens on.
SERVER_PORT=8080

# Log level for logrus (debug, info, warn, error).
LOG_LEVEL=info

# Target environment label used in logs and Sentry (development, staging, production, etc.).
ENV=development

# URL for the LLM endpoint (e.g., OpenRouter endpoint).
LLM_ENDPOINT=https://openrouter.ai/api/v1/chat/completions

# API key used to authenticate with the LLM provider.
LLM_API_KEY=your-openrouter-api-key

# JSON configuration for LLM models.
# Accepts either a JSON array (e.g., ["anthropic/claude-3.5-sonnet"]) or
# an object with a `models` field to enable fallback routing.
# Example given below:
# LLM_MODELS={"models":["anthropic/claude-3.5-sonnet","gryphe/mythomax-l2-13b"]}
LLM_MODELS=

# Sentry DSN for error reporting. Leave blank to disable Sentry.
SENTRY_DSN=

# Set to 1 to enable live LLM tests during dev.
LLM_LIVE_TEST=0