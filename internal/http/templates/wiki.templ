package templates

templ WikiPage(data WikiPageData) {
    @AppLayout(data.Title, "") {
        <article class="prose prose-slate max-w-none">
            @RawHTML(data.HTML)
        </article>
    }
}

